/**
 * mockData.ts — DELETED
 *
 * This file contained static mock data for a Vercel demo deployment that
 * no longer exists. The backend is live (FastAPI + PostgreSQL + Neo4j).
 * All data comes from real API calls in api.ts.
 *
 * If a demo/offline mode is ever needed again, mock at the API layer
 * using MSW (Mock Service Worker) rather than duplicating data here.
 */
export {};
